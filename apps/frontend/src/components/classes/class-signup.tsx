import { H1, H2, H3, A} from '../generic/styled-tags';

export default function ClassSignup () {
    return (
        <section>
            <H2>sign up</H2>
            <p className="text-body2 font-brandon">text and cards</p>
        </section>
    );
}
    
